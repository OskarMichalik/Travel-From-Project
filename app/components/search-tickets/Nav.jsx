import classes from "./Nav.module.css";

export default function Nav({ fromInfo, toInfo, departureInfo, returnInfo }) {
  // similar to the input but no onclick and scroll or smth
  return <div className={classes.nav}>nav</div>;
}
